# 📋 План развития ProjectT

**Дата:** 28 мая 2026 (обновлено)
**Текущая версия:** v0.1.0 (beta)
**Целевая версия:** v0.3.0+

---

## 🔍 Анализ текущего состояния проекта

### Что уже реализовано

| Компонент | Статус | Комментарии |
|-----------|--------|-------------|
| Карточки (items) с контентом | ✅ Работает | Файлы, текст, ссылки, изображения |
| Система тегов | ✅ Работает | Цветные, автодополнение, фильтрация |
| SQLite хранилище | ✅ Работает | Метаданные, миграции |
| Файловое хранилище (CAS) | ✅ Работает | SHA-256 хеши, дедупликация |
| P2P сеть (libp2p) | ✅ Работает | DHT, mDNS, bootstrap |
| 1-on-1 чат | ✅ Работает | Шифрование, подписи Ed25519 |
| Передача файлов | ✅ Работает | Прямая между пирами |
| Обмен профилями | ✅ Работает | Автообмен при подключении |
| Синхронизация элементов | ✅ Работает | Протокол itemsync |
| Обмен аватарами | ✅ Работает | |
| Helper mode | ✅ Работает | Хранение адресов |
| Autodial | ✅ Работает | Автопереподключение |
| Peer exchange | ✅ Работает | Обмен адресами |
| UI (Fyne) | ✅ Работает | Telegram-style редизайн завершён |
| Групповые чаты (backend) | ✅ Работает | Протокол, PubSub, sync, invites, admin, БД — реализовано. UI интеграция pending |
| CI/CD | ✅ Работает | GitHub Actions: lint, test, build |
| Тесты | ✅ Работает | 30+ тестовых файлов, race detection, coverage |
| Prometheus мониторинг | ✅ Работает | Метрики, Grafana |

### Известные проблемы

| Проблема | Приоритет | Влияние |
|----------|-----------|---------|
| Логирование через fmt.Println/log.Printf | Средний | Отладка |
| Нет системных уведомлений | Средний | UX |
| Групповые чаты без UI | Высокий | Фича не доступна пользователям |

---

## 🎯 План реализации по направлениям

---

### ~~1. 🧪 Тестирование P2P соединения из разных сетей~~ ✅ Частично реализовано

**Статус:** CI тесты настроены, p2p_test.go существует. Тестирование из разных сетей — требует физических устройств.

#### Оставшиеся задачи

1. **Диагностические инструменты**
   - [ ] Команда `p2p status` — показать текущее состояние сети
   - [ ] Визуальный индикатор типа подключения (direct/relay/dht)
   - [ ] Тест скорости соединения между пирами

2. **Файл логирования** (перенесено в задачу 5)
   - [ ] Заменить `fmt.Println`/`log.Printf` на структурированный логгер (zap/slog)
   - [ ] Уровни: debug, info, warn, error
   - [ ] Ротация логов (файл + консоль)
   - [ ] Отдельный лог для P2P событий

---

### 2. 💬 Групповые чаты — UI интеграция

**Сложность:** ⭐⭐⭐⭐ (выше средней)
**Время:** 2-3 недели

**Backend уже реализован:**
- ✅ Протокол `/projectt/groupchat/1.0.0`
- ✅ PubSub менеджер (go-libp2p-pubsub)
- ✅ Сервис инвайтов `/projectt/groupchat-invite/1.0.0`
- ✅ Сервис синхронизации истории `/projectt/groupchat-sync/1.0.0`
- ✅ Админ-сервис `/projectt/groupchat-admin/1.0.0`
- ✅ Верификация участников
- ✅ Модели: `GroupChat`, `GroupMember`, `GroupMessage`, `GroupInvite`
- ✅ БД таблицы: `group_chats`, `group_members`, `group_messages`, `group_invites`
- ✅ Миграции и индексы

#### Что нужно сделать — UI

1. **Список групповых чатов в sidebar**
   - [ ] Отображение групповых чатов рядом с 1-on-1 чатами
   - [ ] Иконка группы vs личного чата
   - [ ] Превью последнего сообщения
   - [ ] Счётчик непрочитанных

2. **Создание группы**
   - [ ] Кнопка "Создать группу" в списке чатов
   - [ ] Диалог: название, описание, аватар
   - [ ] Выбор участников из контактов (по PeerID)

3. **Окно приглашения участников**
   - [ ] Генерация invite-токена
   - [ ] Копирование токена в буфер обмена
   - [ ] Вход в группу по токену

4. **UI чата группы**
   - [ ] Отображение имени отправителя в сообщении
   - [ ] Аватары участников рядом с сообщениями
   - [ ] Информация о группе (правая панель)
   - [ ] Список участников с ролями

5. **Управление группой (для админа)**
   - [ ] Изменение названия/описания
   - [ ] Исключение участников
   - [ ] Назначение ролей (admin/moderator/member)
   - [ ] Удаление группы

---

### ~~3. 🎨 Редизайн интерфейса в стиле Telegram~~ ✅ Реализовано

**Статус:** Редизайн завершён. Сайдбар сворачивается, чаты в Telegram-style, пузырьки сообщений, навигация.

---

### 4. 🖥️ Кроссплатформенный выбор файлов

**Сложность:** ⭐⭐ (низкая)
**Время:** 3-5 дней

#### Что нужно сделать

1. **Определение операционной системы**
   - [ ] Использовать `runtime.GOOS` для определения ОС
   - [ ] Поддержка: Windows, macOS, Linux
   - [ ] Абстракция через интерфейс `FilePicker`

2. **Кроссплатформенный файловый диалог**
   - [ ] Windows: `dialog.File()` (уже работает)
   - [ ] macOS: нативный NSSavePanel/NSOpenPanel через Fyne
   - [ ] Linux: GTK file chooser / xdg-open fallback
   - [ ] Fyne уже предоставляет `dialog.NewFileOpen` — проверить кроссплатформенность

3. **Кастомный файловый браузер (опционально)**
   - [ ] Встроенный UI для навигации по файловой системе
   - [ ] Иконки типов файлов
   - [ ] Быстрый доступ к домашней папке, рабочему столу, документам
   - [ ] История недавних папок

#### Технические детали

```go
// Абстракция файлового диалога
type FilePicker interface {
    OpenFile(filters ...*dialog.FileFilter) (fyne.URIReadCloser, error)
    SaveFile(filters ...*dialog.FileFilter) (fyne.URIWriteCloser, error)
    OpenDirectory() (fyne.URI, error)
}

// Определение ОС
func GetOS() string {
    return runtime.GOOS // "windows", "darwin", "linux"
}
```

---

### 5. 📡 Фоновый режим и системный трей

**Сложность:** ⭐⭐⭐ (средняя)
**Время:** 1-2 недели

#### Что нужно сделать

1. **Системный трей (systray)**
   - [ ] Иконка в трее с контекстным меню
   - [ ] Пункты меню: "Открыть", "Статус P2P", "Выход"
   - [ ] Зависимость: `fyne.io/systray` или `getlantern/systray`

2. **Сворачивание в трей**
   - [ ] При закрытии окна — не завершать процесс, а скрыть окно
   - [ ] Настройка: "Закрывать в трей" vs "Завершать приложение"
   - [ ] Двойной клик по иконке в трее — восстановить окно

3. **Фоновая работа P2P**
   - [ ] P2P сеть продолжает работать в фоне
   - [ ] Приём сообщений и файлов без открытого окна
   - [ ] Уведомления о новых сообщениях через systray
   - [ ] Синхронизация элементов в фоне

4. **Уведомления**
   - [ ] Всплывающие уведомления systray при новых сообщениях
   - [ ] Уведомления о завершении передачи файлов
   - [ ] Уведомления о подключении/отключении пиров
   - [ ] Настройка: какие уведомления показывать

5. **Настройки фонового режима**
   - [ ] Вкл/выкл фоновый режим
   - [ ] "Запускать при загрузке системы" (опционально)
   - [ ] Лимиты на фоновую активность (трафик, CPU)

#### Технические детали

```go
// Сворачивание в трей
func (app *App) minimizeToTray() {
    app.window.Hide()
    systray.SetIcon(icon)
    systray.SetTitle("ProjectT")
    systray.SetTooltip("ProjectT — работает в фоне")
    
    systray.AddMenuItem("Открыть", "Показать окно").Click(func() {
        app.window.Show()
        app.window.RequestFocus()
    })
    systray.AddMenuItem("Выход", "Завершить приложение").Click(func() {
        app.cleanup()
        systray.Quit()
    })
}

// Определение ОС для автозапуска
func setupAutostart(enabled bool) error {
    switch runtime.GOOS {
    case "windows":
        return setupWindowsAutostart(enabled)
    case "darwin":
        return setupMacOSAutostart(enabled)
    case "linux":
        return setupLinuxAutostart(enabled)
    }
}
```

---

### 6. 📂 Навигация по файловой системе ПК

**Сложность:** ⭐⭐⭐⭐ (выше средней)
**Время:** 2-3 недели

#### Что нужно сделать

1. **Файловый браузер внутри приложения**
   - [ ] Отображение реальной файловой системы (дерево папок)
   - [ ] Навигация: переход в папки, возврат назад, хлебные крошки
   - [ ] Отображение файлов с иконками по типу
   - [ ] Поиск по файловой системе

2. **Интеграция с БД приложения**
   - [ ] Таблица `external_files` — ссылки на файлы вне хранилища
   - [ ] Таблица `external_folders` — отслеживаемые папки
   - [ ] Метаданные: путь, размер, дата изменения, хеш (опционально)
   - [ ] Пометки/теги на внешние файлы

3. **Операции с файлами**
   - [ ] Открытие файла из приложения (через системный обработчик)
   - [ ] Добавление пометки/тега на любой файл системы
   - [ ] "Закрепить" файл — быстрый доступ из sidebar
   - [ ] Перемещение файла в хранилище приложения (копирование)
   - [ ] Отправка файла пиру без копирования в хранилище (по ссылке)

4. **Индексация**
   - [ ] Сканирование выбранных папок
   - [ ] Кэширование метаданных для быстрого поиска
   - [ ] Watcher — отслеживание изменений в реальном времени (fsnotify)
   - [ ] Инкрементальное обновление при изменении файлов

5. **UI интеграция**
   - [ ] Новая вкладка "Файлы" в sidebar или навигации
   - [ ] Переключатель: "Хранилище приложения" ↔ "Файловая система"
   - [ ] Контекстное меню для файлов: открыть, пометить, отправить, скопировать
   - [ ] Drag-and-drop из файлового браузера в чат

6. **Безопасность**
   - [ ] Запрос подтверждения при первом сканировании папки
   - [ ] Не копировать файлы без явного действия пользователя
   - [ ] Хранить только пути и метаданные, не содержимое
   - [ ] Возможность удалить индексированную папку

#### Технические детали

```go
// Модель внешнего файла
type ExternalFile struct {
    ID           int       `json:"id"`
    Path         string    `json:"path"`          // Абсолютный путь
    Name         string    `json:"name"`
    Size         int64     `json:"size"`
    ModTime      time.Time `json:"mod_time"`
    Hash         string    `json:"hash"`          // Опционально
    Tags         []string  `json:"tags"`          // Пометки пользователя
    IsPinned     bool      `json:"is_pinned"`
    IndexedAt    time.Time `json:"indexed_at"`
}

// Watcher для отслеживания изменений
type FileSystemWatcher struct {
    watcher    *fsnotify.Watcher
    watchedFolders []string
    onChange   func(path string, event fsnotify.Op)
}

// Файловый браузер
type FileSystemBrowser struct {
    widget.BaseWidget
    currentPath string
    tree        *widget.Tree
    onFileSelect func(path string)
}
```

#### Зависимости
- Кроссплатформенный выбор файлов (задача 4) — полезно для единообразия
- Фоновый режим (задача 5) — для watcher в фоне

---

### 7. 🎬 Медиа превью и превью папок

**Сложность:** ⭐⭐⭐ (средняя)
**Время:** 1-2 недели

#### Что нужно сделать

1. **Превью для видео файлов**
   - [ ] Генерация thumbnails из видео (первый кадр / средний кадр)
   - [ ] Поддерживаемые форматы: MP4, WebM, AVI, MKV
   - [ ] Использование `ffmpeg` или встроенных средств Go для извлечения кадра
   - [ ] Отображение thumbnail в карточке элемента
   - [ ] Индикатор типа видео на превью (иконка play)
   - [ ] Длительность видео в мета-информации

2. **Протестировать аудио файлы**
   - [ ] Проверка текущей реализации отображения аудио
   - [ ] Воспроизведение аудио из приложения (плеер)
   - [ ] Отображение метаданных: название, исполнитель, альбом, длительность
   - [ ] Обложка альбома как превью (если встроена в файл)
   - [ ] Поддерживаемые форматы: MP3, FLAC, OGG, WAV
   - [ ] Тестирование на разных ОС (Windows, macOS, Linux)

3. **Превью для папок**
   - [ ] Генерация composite-превью из содержимого папки (сетка 2x2 из thumbnails содержимого)
   - [ ] Если папка пуста — стандартная иконка папки
   - [ ] Если папка содержит изображения — показать первые 4 изображения
   - [ ] Если папка содержит смешанный контент — приоритет: изображения > видео > документы
   - [ ] Отображение количества элементов в папке
   - [ ] Обновление превью при изменении содержимого папки

4. **Оптимизация генерации превью**
   - [ ] Кэширование thumbnails в файловом хранилище
   - [ ] Асинхронная генерация (не блокировать UI)
   - [ ] Lazy loading — генерировать только для видимых элементов
   - [ ] Ограничение размера thumbnail (например, 256x256)

#### Технические детали

```go
// Генерация видео thumbnail
func GenerateVideoThumbnail(videoPath string, outputPath string, timestamp time.Duration) error {
    // Вариант 1: ffmpeg через exec
    cmd := exec.Command("ffmpeg", "-i", videoPath, 
        "-ss", fmt.Sprintf("%v", timestamp),
        "-vframes", "1", "-q:v", "2", outputPath)
    return cmd.Run()
    
    // Вариант 2: pure Go через go-ffmpeg или аналог
}

// Composite превью папки
type FolderPreview struct {
    FolderID    int      `json:"folder_id"`
    Thumbnails  []string `json:"thumbnails"`  // Пути к 4 thumbnail'ам
    ItemCount   int      `json:"item_count"`
    GeneratedAt time.Time `json:"generated_at"`
}

func GenerateFolderPreview(folderID int) (*FolderPreview, error) {
    items, _ := queries.GetItemsInFolder(folderID)
    var thumbs []string
    for _, item := range items {
        if len(thumbs) >= 4 {
            break
        }
        if item.Type == models.ItemTypeImage {
            thumb := generateThumbnail(item.FileHash)
            thumbs = append(thumbs, thumb)
        }
    }
    return &FolderPreview{
        FolderID:   folderID,
        Thumbnails: thumbs,
        ItemCount:  len(items),
    }, nil
}

// Аудио метаданные
type AudioMetadata struct {
    Title    string `json:"title"`
    Artist   string `json:"artist"`
    Album    string `json:"album"`
    Duration int    `json:"duration"` // в секундах
    CoverPath string `json:"cover_path"` // путь к обложке
}

func ExtractAudioMetadata(filePath string) (*AudioMetadata, error) {
    // Использовать github.com/dhowden/tag для чтения ID3/FLAC tags
    f, _ := os.Open(filePath)
    defer f.Close()
    m, _ := tag.ReadFrom(f)
    return &AudioMetadata{
        Title:    m.Title(),
        Artist:   m.Artist(),
        Album:    m.Album(),
    }, nil
}
```

#### Зависимости
- Файловое хранилище (CAS) — уже реализовано
- Для ffmpeg: проверка наличия в системе или bundling

---

### 8. 🪙 Криптовалюта для вознаграждения пользователей

**Сложность:** ⭐⭐⭐⭐⭐⭐ (очень высокая)
**Время:** 6-10 недель

#### Что нужно сделать

##### 4.1. Создание собственной криптовалюты

**Вариант A: Токен на существующем блокчейне (рекомендуется)**
- Создать токен на базе Ethereum (ERC-20) или TON
- Плюсы: готовая инфраструктура, кошельки, биржи
- Минусы: зависимость от внешней сети, комиссии

**Вариант B: Собственный блокчейн**
- Написать свой консенсус (PoS/PoW)
- Плюсы: полный контроль
- Минусы: огромная сложность, безопасность

**Вариант C: Внутренняя система баллов (рекомендуется для начала)**
- Не блокчейн, а внутренняя учётная система
- Плюсы: быстро, не нужен внешний блокчейн
- Минусы: не "настоящая" крипта, нельзя вывести

**Рекомендация:** Начать с Варианта C, потом мигрировать на A

##### 4.2. Внутренняя система баллов (MVP)

1. **Модель данных**
   - [ ] Таблица `wallets` (peer_id, balance, created_at)
   - [ ] Таблица `transactions` (id, from_peer, to_peer, amount, type, timestamp)
   - [ ] Таблица `rewards_log` (id, peer_id, reward_type, amount, timestamp, details)

2. **Типы вознаграждений**

   | Действие | Награда | Описание |
   |----------|---------|----------|
   | Хранение данных | X/час | За каждый ГБ данных других пользователей |
   | Передача данных | X/МБ | За ретрансляцию чужих сообщений/файлов |
   | Обмен профилями | X | За передачу списка контактов новому пиру |
   | Раздача файлов | X/запрос | За ответ на запрос файла из библиотеки |
   | Хранение библиотеки | X/элемент | За хранение общих элементов |
   | Аптайм | X/день | Бонус за время онлайн |

3. **Механизм подсчёта**

   ```go
   type RewardEngine struct {
       db         *sql.DB
       metrics    *MetricsCollector
       config     *RewardConfig
   }

   // Пример: начисление за хранение
   func (r *RewardEngine) CalculateStorageReward(peerID string, bytesStored int64, hours int) int64 {
       gbStored := float64(bytesStored) / 1e9
       rate := r.config.StorageRatePerGBPerHour // например, 0.01 токенов
       return int64(gbStored * rate * float64(hours) * 1000) // в миллитокенах
   }

   // Пример: начисление за передачу
   func (r *RewardEngine) CalculateTransferReward(peerID string, bytesTransferred int64) int64 {
       mbTransferred := float64(bytesTransferred) / 1e6
       rate := r.config.TransferRatePerMB // например, 0.005 токенов
       return int64(mbTransferred * rate * 1000)
   }
   ```

4. **Доказательство работы (Proof of Service)**

   Проблема: как проверить, что пользователь действительно хранит/передаёт данные?

   **Решения:**
   - [ ] Challenge-response: случайная проверка хеша хранимого файла
   - [ ] Подписанные квитанции: получатель подписывает факт получения
   - [ ] Web of Trust: пиры оценивают друг друга
   - [ ] Логирование + консенсус: несколько пиров подтверждают действие

   ```go
   // Квитанция о передаче
   type TransferReceipt struct {
       TransferID    string    `json:"transfer_id"`
       FromPeer      string    `json:"from_peer"`
       ToPeer        string    `json:"to_peer"`
       RelayPeer     string    `json:"relay_peer"`
       Bytes         int64     `json:"bytes"`
       Timestamp     int64     `json:"timestamp"`
       RecipientSig  []byte    `json:"recipient_sig"`  // Подпись получателя
   }
   ```

5. **Защита от злоупотреблений**
   - [ ] Лимиты на начисления в день
   - [ ] Анти-спам: не начислять за передачу одному и тому же пиру
   - [ ] Верификация: случайные проверки хранимых данных
   - [ ] Штрафы: за отказ отдачи данных при запросе
   - [ ] Репутация: рейтинг надёжности пира

##### 4.3. Интеграция с P2P сетью

1. **Протокол учёта** (`/projectt/rewards/1.0.0`)
   - [ ] Передача квитанций о действиях
   - [ ] Синхронизация балансов
   - [ ] Запрос подтверждения от других пиров

2. **Распределённый реестр (опционально)**
   - [ ] Каждый пир хранит копию транзакций
   - [ ] Консенсус для подтверждения транзакций
   - [ ] Merkle tree для верификации

   **Упрощённый вариант:**
   - Баланс хранится локально + подписан
   - При взаимодействии пиры обмениваются подписанными балансами
   - Конфликты разрешаются по timestamp + репутации

##### 4.4. UI для криптовалюты

1. **Кошелёк**
   - [ ] Отображение баланса в сайдбаре или отдельной вкладке
   - [ ] История транзакций
   - [ ] Детализация начислений (за что получено)

2. **Настройки вознаграждений**
   - [ ] Включение/выключение режима "помощника"
   - [ ] Лимиты на хранение (сколько ГБ отдавать под чужие данные)
   - [ ] Лимиты на трафик (сколько МБ/день ретранслировать)

3. **Визуализация**
   - [ ] График заработка
   - [ ] Рейтинг среди пиров (опционально)
   - [ ] Уведомления о начислениях

##### 4.5. Миграция на реальный блокчейн (долгосрочный план)

1. **Выбор платформы**
   - Ethereum (ERC-20) — самая распространённая
   - TON — быстрая, низкие комиссии
   - Solana — высокая пропускная способность

2. **Мост**
   - [ ] Конвертация внутренних баллов в токены
   - [ ] Интеграция с внешним кошельком
   - [ ] Smart contract для выпуска токенов

#### Технические детали

```go
// Модель кошелька
type Wallet struct {
    PeerID    string    `json:"peer_id"`
    Balance   int64     `json:"balance"`     // в миллитокенах
    CreatedAt time.Time `json:"created_at"`
    UpdatedAt time.Time `json:"updated_at"`
    Signature []byte    `json:"signature"`   // подпись баланса
}

// Модель транзакции
type Transaction struct {
    ID        string    `json:"id"`
    FromPeer  string    `json:"from_peer"`
    ToPeer    string    `json:"to_peer"`
    Amount    int64     `json:"amount"`
    Type      string    `json:"type"` // storage, transfer, profile_share, etc.
    Timestamp int64     `json:"timestamp"`
    Signature []byte    `json:"signature"`
}

// Конфигурация ставок
type RewardConfig struct {
    StorageRatePerGBPerHour float64 `yaml:"storage_rate"`    // 0.01
    TransferRatePerMB       float64 `yaml:"transfer_rate"`   // 0.005
    ProfileShareReward      int64   `yaml:"profile_reward"`  // 100
    FileShareReward         int64   `yaml:"file_reward"`     // 50
    UptimeDailyBonus      int64   `yaml:"uptime_bonus"`    // 200
    DailyCap              int64   `yaml:"daily_cap"`       // 10000
}
```

#### Зависимости
- Требуется стабильная P2P сеть (задача 1)
- Требуется система логирования (задача 1)
- Можно начинать после стабилизации базовых функций

#### Риски
- Экономическая модель может потребовать балансировки
- Защита от злоупотреблений — сложная задача
- Юридические аспекты криптовалюты

---

### 5. 🔧 Общая отладка и оптимизация

**Сложность:** ⭐⭐⭐ (средняя)
**Время:** 2-3 недели (постоянно)

#### Что нужно сделать

1. **Логирование**
   - [ ] Переход с `fmt.Println`/`log.Printf` на `slog` (Go 1.21+) или `zap`
   - [ ] Структурированные логи (JSON формат)
   - [ ] Уровни логирования (debug, info, warn, error)
   - [ ] Ротация файлов (max 10MB, 5 файлов)
   - [ ] Отдельные логи для: P2P, UI, DB, rewards

2. **Оптимизация производительности**
   - [ ] Профилирование CPU (`go tool pprof`)
   - [ ] Профилирование памяти (`go tool pprof -alloc`)
   - [ ] Оптимизация медленных переходов между табами
   - [ ] Кэширование результатов запросов к БД
   - [ ] Lazy loading для больших списков

3. **Оптимизация UI**
   - [ ] Виртуализация списков (рендер только видимых элементов)
   - [ ] Кэширование превью изображений
   - [ ] Асинхронная загрузка тяжёлых компонентов
   - [ ] Debounce для поиска

4. **~~Тестирование~~ ✅ Реализовано**
   - ~~Unit тесты для сервисов~~ — 30+ тестовых файлов
   - ~~Integration тесты для P2P протоколов~~ — p2p_test.go, chat_test.go, и др.
   - ~~Тесты на race conditions~~ — `-race` флаг в CI
   - ~~Benchmark для критичных функций~~

5. **~~CI/CD~~ ✅ Реализовано**
   - ~~GitHub Actions для автоматических сборок~~ — `.github/workflows/ci.yml`
   - ~~Автоматический запуск тестов при PR~~ — lint + test + build
   - ~~Автоматический релиз при теге~~ — `.github/workflows/release.yml`
   - ~~Кросс-компиляция (Windows, Linux, macOS)~~

6. **Мелкие улучшения**
   - [ ] Системные уведомления (systray) — уже есть systray в зависимостях!
   - [ ] Горячие клавиши
   - [ ] Drag-and-drop файлов в чат
   - [ ] Emoji picker
   - [ ] Статус "печатает..."
   - [ ] Поиск по содержимому сообщений
   - [ ] Экспорт/импорт контактов
   - [ ] Резервное копирование БД

---

### 9. 📊 Приоритизация и порядок выполнения

#### Фаза 1: Стабилизация + UX (1-2 недели)

| Задача | Время | Приоритет |
|--------|-------|-----------|
| Логирование вместо fmt.Println/log.Printf | 2-3 дня | 🔴 Критично |
| Кроссплатформенный выбор файлов | 3-5 дней | 🟡 Важно |
| Системные уведомления | 2-3 дня | 🟡 Важно |

#### Фаза 2: Фоновый режим (1-2 недели)

| Задача | Время | Приоритет |
|--------|-------|-----------|
| Системный трей (systray) | 3-5 дней | 🔴 Критично |
| Сворачивание в трей | 2-3 дня | 🔴 Критично |
| Фоновая работа P2P | 3-5 дней | 🟡 Важно |
| Уведомления из трея | 2-3 дня | 🟡 Важно |

#### Фаза 3: UI групповых чатов (2-3 недели)

| Задача | Время | Приоритет |
|--------|-------|-----------|
| Список групповых чатов в sidebar | 2-3 дня | 🔴 Критично |
| Создание группы + инвайты | 3-5 дней | 🔴 Критично |
| UI группового чата (имена, аватары) | 1 неделя | 🔴 Критично |
| Управление группой (админ) | 3-5 дней | 🟡 Важно |

#### Фаза 4: Медиа превью (1-2 недели)

| Задача | Время | Приоритет |
|--------|-------|-----------|
| Превью для видео (thumbnails) | 3-5 дней | 🟡 Важно |
| Тестирование аудио файлов | 2-3 дня | 🟡 Важно |
| Превью для папок (composite) | 3-5 дней | 🟡 Важно |
| Оптимизация генерации превью | 2-3 дня | 🟢 Желательно |

#### Фаза 5: Файловая система (2-3 недели)

| Задача | Время | Приоритет |
|--------|-------|-----------|
| Файловый браузер | 1 неделя | 🟡 Важно |
| Интеграция с БД (external_files) | 3-5 дней | 🟡 Важно |
| Индексация + fsnotify watcher | 3-5 дней | 🟡 Важно |
| UI интеграция + операции | 3-5 дней | 🟢 Желательно |

#### Фаза 6: Оптимизация (постоянно)

| Задача | Время | Приоритет |
|--------|-------|-----------|
| Профилирование CPU/памяти | 1 неделя | 🟡 Важно |
| Виртуализация списков | 3-5 дней | 🟡 Важно |
| Мелкие улучшения | постоянно | 🟢 Желательно |

#### Фаза 7: Криптовалюта (6-10 недель)

| Задача | Время | Приоритет |
|--------|-------|-----------|
| Внутренняя система баллов | 2-3 недели | 🔴 Критично |
| Интеграция с P2P | 1-2 недели | 🔴 Критично |
| Proof of Service | 2-3 недели | 🟡 Важно |
| UI кошелька | 1 неделя | 🟡 Важно |
| Защита от злоупотреблений | 1-2 недели | 🟡 Важно |
| Миграция на блокчейн | 2-4 недели | 🟢 Долгосрочно |

---

## 📦 Оценка общей сложности

| Направление | Сложность | Время | Зависимости |
|-------------|-----------|-------|-------------|
| ~~Тестирование P2P~~ | ~~⭐⭐⭐~~ | ~~1-2 нед~~ | ✅ CI настроен |
| UI групповых чатов | ⭐⭐⭐⭐ | 2-3 нед | Backend готов |
| ~~Редизайн UI~~ | ~~⭐⭐⭐⭐~~ | ~~3-4 нед~~ | ✅ Завершён |
| Криптовалюта | ⭐⭐⭐⭐⭐⭐ | 6-10 нед | Стабильный P2P, логирование |
| Логирование (slog) | ⭐⭐ | 2-3 дня | Нет |
| Кроссплатформенный файловый диалог | ⭐⭐ | 3-5 дней | Нет |
| Фоновый режим + трей | ⭐⭐⭐ | 1-2 нед | Нет |
| Навигация по файловой системе | ⭐⭐⭐⭐ | 2-3 нед | Кроссплатформенный диалог |
| Медиа превью (видео/аудио/папки) | ⭐⭐⭐ | 1-2 нед | Файловое хранилище |
| Оптимизация | ⭐⭐⭐ | 2-3 нед | Нет |

**Общее время:** 18-27 недель (4.5-7 месяцев) при работе одного разработчика

---

## ⚠️ Ключевые риски

1. ~~Fyne ограничения~~ ✅ Проверено — Fyne справляется с Telegram-style UI
   - ~~UI фреймворк может не поддерживать все desired элементы Telegram-style~~
   - ~~**Митигация:** Проверить возможности Fyne до начала редизайна, рассмотреть альтернативы (Wails, Fyne + custom canvas)~~

2. ~~libp2p PubSub для групповых чатов~~ ✅ Реализовано
   - ~~PubSub требует хорошего покрытия сети~~
   - ~~**Митигация:** Использовать гибридный подход (PubSub + direct messages)~~

3. **Экономика криптовалюты**
   - Баланс вознаграждений сложно настроить
   - **Митигация:** Начать с внутренней системы, собирать метрики, корректировать

4. **Безопасность**
   - Криптовалюта привлекает злоумышленников
   - **Митигация:** Консервативные лимиты, репутационная система, аудит

5. ~~Тестирование P2P~~ ✅ CI тесты настроены
   - ~~Требует физических устройств в разных сетях~~
   - ~~**Митигация:** Использовать облачные VM, попросить друзей помочь~~

---

## 🛠️ Рекомендуемый стек для новых компонентов

| Компонент | Технология | Статус |
|-----------|------------|--------|
| Логирование | `log/slog` | ❌ Не начато |
| Групповые чаты (backend) | `go-libp2p-pubsub` | ✅ Реализовано |
| UI стилизация | Fyne custom widgets + canvas | ✅ Завершено |
| Групповые чаты (UI) | Fyne custom widgets | ❌ Не начато |
| Файловый диалог | `fyne.io/fyne/v2/dialog` + `runtime.GOOS` | ❌ Не начато |
| Системный трей | `fyne.io/systray` | ❌ Не начато |
| Файловый браузер | `os`, `path/filepath`, `fsnotify` | ❌ Не начато |
| Видео thumbnails | `ffmpeg` или `go-ffmpeg` | ❌ Не начато |
| Аудио метаданные | `github.com/dhowden/tag` | ❌ Не начато |
| Крипто (внутренняя) | Ed25519 + SQLite | ❌ Не начато |
| Крипто (блокчейн) | Ethereum ERC-20 или TON | ❌ Долгосрочно |
| Тестирование | `testing` + `testify` | ✅ 30+ тестов |
| CI/CD | GitHub Actions | ✅ Настроено |

---

## 📝 Чек-лист для начала работы

- [x] Создать ветку `dev` для активной разработки
- [ ] Настроить логирование (slog)
- [ ] Создать файл конфигурации для ставок вознаграждений
- [ ] Подготовить тестовые устройства для P2P
- [x] Определить минимальный набор функций для v0.2
- [x] Создать mockups нового UI (Telegram-style редизайн завершён)
- [x] Написать spec для протокола группового чата (backend реализован)
- [ ] Интегрировать групповые чаты в UI
- [ ] Проверить кроссплатформенность `dialog.NewFileOpen` в Fyne
- [ ] Добавить `fyne.io/systray` в зависимости
- [ ] Спроектировать БД для внешних файлов (`external_files`, `external_folders`)
- [ ] Проверить наличие `ffmpeg` в системе (для видео thumbnails)
- [ ] Протестировать текущую реализацию аудио файлов

---

<div align="center">

**План создан 23 мая 2026. Будет обновляться по мере прогресса.**

*ProjectT — децентрализованное пространство для идей*

</div>
